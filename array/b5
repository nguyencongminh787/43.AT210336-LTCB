#include<stdio.h>
int main(){
    int n,x;
    int count = 0;
    scanf("%d%d", &n,&x);
    int arr[n];
    for(int i = 0; i <n ; i++){
        scanf("%d", &arr[i]);
        if(x == arr[i]){
            count++;
        }
    }printf("số lần xuất hiện của %d là : %d\n", x,count);
}
