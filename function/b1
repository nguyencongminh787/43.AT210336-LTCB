#include<stdio.h>
#include<math.h>
int tinh(int x, int y){
    int k = (int)pow(x,y);
    printf("ket qua la : %d\n", k);
    return k;}
    int main(){ 
        int x,y;
        printf("nhap 2 so x va y:");
    scanf("%d%d",&x,&y);
        tinh(x, y);
        return 0;}
